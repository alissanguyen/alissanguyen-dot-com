export type Tech = {
  name: string;
  icon: string;
};
export enum AlertType {
  ERROR = "error",
  SUCCESS = "success",
  CONFIRMED = "confirmed"
}

export enum SupportedTheme {
  DARK = "dark",
  LIGHT = "light"
}

